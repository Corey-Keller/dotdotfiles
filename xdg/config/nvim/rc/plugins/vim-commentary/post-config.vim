" Post-plugin config for 'vim-commentary'
" This file is for anything that must be set AFTER the plugin is sourced.

command! DoCommentLine execute "normal \<Plug>CommentaryLine"
command! DoUncommentLine execute "normal \<Plug>Commentary\<Plug>Commentary"

" vim:set filetype=vim:
